# ChatBI 稳定性测试报告（2026-03-20）

## 结论
- 当前版本可用于领导汇报和内部演示。
- 本轮新增库存能力已接入完成，主回归和库存专项测试通过。
- 本轮未发现新的阻塞性错误；历史错误日志为旧版本遗留记录。

## 本轮修复
1. 新增库存快照表 `inventory_stock`，并自动生成库存样例数据。
2. 新增库存相关语义：可售库存、在库量、在途库存、库存金额、缺货SKU数、仓库名称、仓库类型、库存状态、快照日期等。
3. 强化原始数据质量巡检，将库存口径纳入巡检与修复。
4. 修复“支付买家数”默认状态过滤追加位置错误，避免首轮 SQL 生成后因第二个 WHERE 触发 SQL repair。
5. 同步重建语义检索文档与向量。

## 数据与语义现状
- `inventory_stock = 4320`
- `semantic_search_doc = 144`
- `semantic_metric = 20`
- `semantic_dimension = 38`
- `semantic_synonym = 50`
- `semantic_example = 14`
- `pending_embeddings = 0`
- 最近一次数据质量巡检：`run_id = 20`，`issue_count = 0`

## 主回归结果
- 回归脚本：`/tmp/chatbi_regression.py`
- 结果：`29 / 29` 全部通过

覆盖点：
- 主页面加载
- 语义后台页面加载
- 报告后台页面加载
- 语义后台 bootstrap
- 报告后台 bootstrap
- 报告模板列表
- 百炼查询链路
- DeepSeek 查询链路
- 澄清问题链路
- 澄清后继续查询
- 连续对话继承链路
- 时间追问继承维度
- 复杂 GMV / 退款率 查询
- 会话日志接口
- 明细导出 CSV
- 图表导出 Word
- 图表 Word 图片嵌入
- 异步报告任务提交 / 完成 / 下载
- 异步报告图片嵌入
- 报告历史列表 / 详情
- 默认模板 TXT / DOCX 下载
- 非法文本模板上传拦截
- 语义一键刷新提交 / 完成

## 库存专项测试
1. 问题：按销售大区和品牌统计当前可售库存、在途库存和库存金额，按库存金额降序
- 结果：成功
- 行数：54
- 维度：销售大区、品牌
- 指标：可售库存、在途库存、库存金额

2. 问题：按销售渠道统计当前可售库存和缺货SKU数，按缺货SKU数降序
- 结果：成功
- 行数：7
- 维度：销售渠道
- 指标：可售库存、缺货SKU数

## 日志检查
- `web.error.log`：本轮未新增错误，当前文件内为历史旧版本记录。
- `worker.error.log`：本轮未新增错误，当前文件内为早期 MySQL 未启动时的旧记录。

## 风险说明
1. 当前仍是单体服务，适合本地和内部演示，不建议直接按生产高并发来承诺。
2. 语义层目前仍属于“强提示 + 候选约束”，不是完全可执行语义层。
3. 如果要继续提升稳定性，下一步建议把核心指标（GMV、退款率、支付买家数、库存金额）升级成可执行语义指标。
